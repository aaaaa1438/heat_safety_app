name: heat_safety_app
description: 高溫工地健康監測 App - 定時飲水提醒與身體自測
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^17.2.2
  shared_preferences: ^2.2.3
  sensors_plus: ^4.0.2
  url_launcher: ^6.3.0
  intl: ^0.19.0
  cupertino_icons: ^1.0.6
  file_picker: ^8.0.0+1
  path_provider: ^2.1.3
  audioplayers: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
